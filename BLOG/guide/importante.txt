=== gradle ===
\Android\sdk\tools\templates\gradle\wrapper NOT FOUND quando viene lanciata l'app sul devices...

estrarre il file templates.zip nella directory
\Android\sdk\tools\

=== ionic native ===
Se ci sono problemi con plugins NATIVE ciò dipende dalle versioni:
npm uninstall --save @ionic-native/core
npm install --save @ionic-native/core@latest

La prima volta che si crea un'app è sempre necessario installare ionic-native

Ogni provider di ionic-native deve essere inserito in app.module.ts in "providers"

=== Optimize Ionic 2 App for Production ===
app/main.ts

<code>
	import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';
	import {enableProdMode} from '@angular/core';

	import { AppModule } from './app.module';

	// this is the magic wand
	enableProdMode();

	platformBrowserDynamic().bootstrapModule(AppModule);
</code>

$ ionic build android --prod --release

=== Optimize Angular for Production ===
# these are equivalent
ng build --target=production --environment=prod
ng build --prod --env=prod
ng build --prod
# and so are these
ng build --target=development --environment=dev
ng build --dev --e=dev
ng build --dev
ng build

=== START APP IONIC ANGULAR ===
$ ionic start ravelloBirraCup sidemenu --type=ionic-angular

Tutti i comandi di ionic-cli "ionic" sono cambiati in "ionic cordova..."

https://cordova.apache.org/announcements/2017/04/05/android-release.html

---

https://services.gradle.org/distributions/gradle-3.5-bin.zip
GRADLE_HOME
D:\Development\gradle-3.5\bin

Aggiungere GRADLE_HOME in PATH